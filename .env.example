# Knowvia Environment Variables
# Copy this file to .env and customize

# JWT Secret (change in production!)
JWT_SECRET=knowvia-dev-secret-change-in-production

# Backend port
PORT=3001

# Database (SQLite is embedded, no config needed)
# For future PostgreSQL migration:
# DATABASE_URL=postgresql://user:password@localhost:5432/knowvia_db
