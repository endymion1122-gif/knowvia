import SwiftUI

enum AppTheme {
    // Concept A: Path-based Academic Minimalism
    static let deepIndigo = Color(hex: "#1B1E5F")
    static let knowledgeBlue = Color(hex: "#243B6B")
    static let slateBlue = Color(hex: "#2F3A5F")
    static let softViolet = Color(hex: "#7B60FF")
    static let orbitBlue = Color(hex: "#449CFF")
    static let mistPurple = Color(hex: "#A78BFA")
    static let paleLavender = Color(hex: "#EDE9FE")
    static let pathTeal = Color(hex: "#21C7C2")
    static let softTeal = Color(hex: "#8FE3D7")
    static let paleMint = Color(hex: "#E6FAF7")
    static let warmWhite = Color(hex: "#FAFAFB")
    static let pageBackground = Color(hex: "#F7F8FA")
    static let cardBackground = Color.white
    static let coolGray = Color(hex: "#E6E8EF")
    static let primaryText = Color(hex: "#111827")
    static let secondaryText = Color(hex: "#6B7280")
    static let tertiaryText = Color(hex: "#9CA3AF")

    // Concept D: Warm Premium Lifelong Learning
    static let warmIvory = Color(hex: "#F7F4EF")
    static let softPlum = Color(hex: "#6B4A6F")
    static let amberAccent = Color(hex: "#EAA23B")
    static let seaGlassTeal = Color(hex: "#4DA6A1")
    static let midnightBlue = Color(hex: "#0F1B2E")

    // Reserved for a later dark mode pass.
    static let darkBackground = Color(hex: "#101218")
    static let darkCard = Color(hex: "#171A22")
    static let darkSidebar = Color(hex: "#202432")
    static let darkBorder = Color(hex: "#303545")
    static let darkPrimaryText = Color(hex: "#F5F7FA")
    static let darkSecondaryText = Color(hex: "#A0A7B5")
}
